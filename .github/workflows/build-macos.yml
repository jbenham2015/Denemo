name: Build Denemo for macOS

on:
  push:
    branches:
      - master
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Cache MacPorts
        id: cache-macports
        uses: actions/cache@v4
        with:
          path: /opt/local
          key: macports-${{ runner.os }}-${{ hashFiles('.github/macports-deps.txt') }}

      - name: Install MacPorts
        if: steps.cache-macports.outputs.cache-hit != 'true'
        run: |
          curl -LO https://github.com/macports/macports-base/releases/download/v2.9.3/MacPorts-2.9.3-13-Ventura.pkg
          sudo installer -pkg MacPorts-2.9.3-13-Ventura.pkg -target /

      - name: Add MacPorts to PATH
        run: |
          echo /opt/local/bin >> $GITHUB_PATH
          echo /opt/local/sbin >> $GITHUB_PATH

      - name: Install dependencies
        if: steps.cache-macports.outputs.cache-hit != 'true'
        run: |
          sudo port install \
            gtk3 +quartz \
            librsvg +quartz \
            gtksourceview3 +quartz \
            evince +quartz \
            guile \
            aubio \
            portaudio \
            fftw-3 \
            fluidsynth \
            libsmf \
            libsndfile \
            libxml2 \
            portmidi \
            rubberband \
            gettext \
            intltool \
            autoconf \
            automake \
            libtool \
            pkgconfig \
            lilypond

      - name: Build Denemo
        run: |
          ./autogen.sh
          mkdir -p build && cd build
          ../configure --prefix=/opt/local
          make -j$(sysctl -n hw.ncpu)

      - name: Install to staging
        run: |
          cd build
          make DESTDIR=${{ github.workspace }}/staging install

      - name: Create .app bundle
        run: |
          APP=Denemo.app
          mkdir -p $APP/Contents/MacOS
          mkdir -p $APP/Contents/Resources

          cp staging/opt/local/bin/denemo $APP/Contents/MacOS/Denemo
          cp -r staging/opt/local/share/denemo $APP/Contents/Resources/

          python3 -c "
          plist = '''<?xml version=\"1.0\" encoding=\"UTF-8\"?>
          <!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\"
            \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">
          <plist version=\"1.0\">
          <dict>
            <key>CFBundleName</key>
            <string>Denemo</string>
            <key>CFBundleDisplayName</key>
            <string>Denemo</string>
            <key>CFBundleIdentifier</key>
            <string>org.denemo.Denemo</string>
            <key>CFBundleVersion</key>
            <string>2.6.49</string>
            <key>CFBundleShortVersionString</key>
            <string>2.6.49</string>
            <key>CFBundleExecutable</key>
            <string>Denemo</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>NSHighResolutionCapable</key>
            <true/>
            <key>NSPrincipalClass</key>
            <string>NSApplication</string>
          </dict>
          </plist>'''
          with open('$APP/Contents/Info.plist', 'w') as f:
              f.write(plist)
          "

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          mkdir -p dist
          mv Denemo.app dist/

          create-dmg \
            --volname "Denemo" \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Denemo.app" 150 200 \
            --hide-extension "Denemo.app" \
            --app-drop-link 450 200 \
            "Denemo-2.6.49.dmg" \
            "dist/" || true

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: Denemo-macOS-dmg
          path: "*.dmg"
          retention-days: 30

      - name: Attach DMG to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: "*.dmg"
